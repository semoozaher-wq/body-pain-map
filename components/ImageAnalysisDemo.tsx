import React, { useRef, useState } from 'react';
import { Platform, Pressable, StyleSheet, Text, View } from 'react-native';
export function ImageAnalysisDemo() { const [result, setResult] = useState(''); const input = useRef<any>(null); const choose = () => { if (Platform.OS === 'web') input.current?.click(); else setResult('في تطبيق الهاتف يمكنك ربط expo-image-picker لاحقًا؛ النسخة الحالية لا ترفع الصور.'); }; return <View style={styles.card}><Text style={styles.title}>تحليل صورة تجريبي</Text><Text style={styles.hint}>اختيار الصورة محلي فقط؛ لا يتم رفعها أو إرسالها لأي خدمة.</Text>{Platform.OS === 'web' ? <input ref={input} type="file" accept="image/*" style={{ display: 'none' }} onChange={() => setResult('تم اختيار الصورة محليًا. التحليل الآلي الحقيقي يحتاج خدمة Vision ومفتاحًا على خادم آمن.')} /> : null}<Pressable onPress={choose} style={styles.button}><Text style={styles.buttonText}>صوّر/اختر مكان الوجع</Text></Pressable>{result ? <Text style={styles.result}>{result}</Text> : null}<Text style={styles.disclaimer}>تحليل مبدئي تجريبي وليس تشخيصًا طبيًا.</Text></View>; }
const styles = StyleSheet.create({ card: { backgroundColor: '#F2F5FF', borderRadius: 18, padding: 16, marginTop: 14, borderWidth: 1, borderColor: '#CBD5F4' }, title: { color: '#34458A', textAlign: 'right', fontWeight: '900', fontSize: 17 }, hint: { color: '#5F6A91', textAlign: 'right', lineHeight: 20, marginTop: 5 }, button: { backgroundColor: '#34458A', borderRadius: 12, padding: 13, alignItems: 'center', marginTop: 11 }, buttonText: { color: '#FFF', fontWeight: '900' }, result: { color: '#364267', textAlign: 'right', lineHeight: 22, marginTop: 12 }, disclaimer: { color: '#7882A5', fontSize: 11, textAlign: 'center', marginTop: 10 } });
