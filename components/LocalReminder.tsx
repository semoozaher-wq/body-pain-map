import React, { useEffect, useState } from 'react';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { Pressable, StyleSheet, Text, TextInput, View } from 'react-native';
const KEY = 'bodymap-reminder-v1';
export function LocalReminder() { const [value, setValue] = useState(''); const [saved, setSaved] = useState(false); useEffect(() => { AsyncStorage.getItem(KEY).then((v) => v && setValue(v)).catch(() => undefined); }, []); const save = () => { AsyncStorage.setItem(KEY, value).catch(() => undefined); setSaved(true); }; return <View style={styles.card}><Text style={styles.title}>تذكير محلي</Text><Text style={styles.hint}>اكتب موعدًا أو تذكيرًا؛ يتم حفظه على جهازك فقط.</Text><TextInput value={value} onChangeText={(v) => { setValue(v); setSaved(false); }} placeholder="مثال: راجع الطبيب غدًا" placeholderTextColor="#8A9AA5" style={styles.input} textAlign="right" /><Pressable onPress={save} style={styles.button}><Text style={styles.buttonText}>{saved ? 'تم الحفظ' : 'حفظ التذكير'}</Text></Pressable></View>; }
const styles = StyleSheet.create({ card: { backgroundColor: '#F5F8FB', borderRadius: 18, padding: 16, marginTop: 14, borderWidth: 1, borderColor: '#D9E4EA' }, title: { color: '#203745', textAlign: 'right', fontWeight: '900', fontSize: 17 }, hint: { color: '#657781', textAlign: 'right', fontSize: 12, marginTop: 5 }, input: { backgroundColor: '#FFF', borderRadius: 12, padding: 12, marginTop: 11, borderWidth: 1, borderColor: '#D9E4EA', color: '#203745' }, button: { backgroundColor: '#0E7C86', borderRadius: 12, padding: 12, alignItems: 'center', marginTop: 9 }, buttonText: { color: '#FFF', fontWeight: '900' } });
