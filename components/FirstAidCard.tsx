import React, { useState } from 'react';
import { Pressable, StyleSheet, Text, View } from 'react-native';
export function FirstAidCard({ area, urgent }: { area: string; urgent: boolean }) { const [open, setOpen] = useState(false); const steps = ['اجلس أو استلقِ في وضع مريح وتنفّس ببطء.', 'اشرب ماءً إن كان ذلك مناسبًا لك وتجنب الحركة التي تزيد الألم.', 'راقب الأعراض وسجّل شدتها ووقت ظهورها.']; return <View style={styles.card}><Pressable onPress={() => setOpen(!open)}><Text style={styles.title}>تمارين إسعاف تعليمية لمنطقة {area} {open ? '⌃' : '⌄'}</Text></Pressable>{open && <><Text style={styles.note}>{urgent ? 'لا تستبدل هذه الخطوات بطلب المساعدة العاجلة عند وجود علامة خطر.' : 'توقف فورًا إذا زاد الألم.'}</Text>{steps.map((step, i) => <Text key={step} style={styles.step}>{i + 1}. {step}</Text>)}<Text style={styles.disclaimer}>تمارين تعليمية عامة وليست علاجًا أو وصفة طبية.</Text></>}</View>; }
const styles = StyleSheet.create({ card: { backgroundColor: '#F3FAF8', borderRadius: 18, padding: 16, marginTop: 14, borderWidth: 1, borderColor: '#BCE5DE' }, title: { color: '#0E6972', textAlign: 'right', fontWeight: '900', fontSize: 16 }, note: { color: '#8D5A00', textAlign: 'right', lineHeight: 21, marginTop: 12 }, step: { color: '#365762', textAlign: 'right', lineHeight: 25, marginTop: 7 }, disclaimer: { color: '#71808C', fontSize: 11, textAlign: 'center', marginTop: 12 } });
