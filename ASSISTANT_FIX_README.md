# إصلاح المساعد الذكي — BodyMap Pain

## المشكلة التي أبلغت عنها
عند كتابة «حاسس بألم في بطني على شمال السرة»، كان التطبيق:
1. يتجاهل الموقع (شمال/يمين/فوق/تحت السرة).
2. يشخّص **ألم عضلي ليفي (فيبروميالجيا M79.67)** وأمراض عضلية فقط.
3. يكرّر **نفس السطرين** في كل الحالات (رعاية ذاتية عامة لا علاقة لها بالألم).

## الحلول المطبّقة (6 حلول جذرية)

### 1) تصنيف الأعراض العامة (Generic) لمنع التضخيم الخاطئ
- كان `hpo:0003326b` (ألم عام) يتطابق مع `hpo:0003326` (ألم عضلي) بسبب قاعدة مطابقة الـ HPO → فكان يرفع كل الأمراض العضلية.
- تمت إعادة تسميته إلى `sym:general-pain` مع وسم `generic: true`، واستبعاد الأعراض العامة من الترجيح.

### 2) فهم موقع الألم داخل البطن (ABDOMEN_LOCATIONS)
- إضافة 7 مواقع: أسفل يمين/يسار السرة، أعلى يمين/يسار، حول السرة، فوق السرة، تحت السرة.
- كل موقع مرتبط بالأعضاء المتوقعة (الزائدة، الأمعاء، المبيض، الكبد، المرارة، المعدة، البنكرياس، المثانة، الرحم).
- مثال: «شمال السرة» → الأمعاء + المبيض. «يمين السرة» → الزائدة + الأمعاء.

### 3) مكتبة أمراض عضوية جديدة (organConditions.json)
- 15 حالة مرتبطة بالأعضاء: الزائدة (K35)، التهاب المعدة (K29)، القرحة (K27)، القولون العصبي (K58)، الرتوج (K57)، النزلة المعوية (A09)، حصى المرارة (K80)، الكبد (K75.9)، البنكرياس (K85)، المسالك (N39.0)، حصى الكلى (N20.0)، كيس المبيض (N83.2)، الانتباذ الرحمي (N80)، الذبحة (I20.9)، الارتجاع (K21.9).

### 4) إعادة كتابة الترجيح (scoreConditions) ليعتمد على الموقع والعضو
- وزن صريح للعضو المذكور = 6، والعضو المستنتج من الموقع = 6 للأول و3 للبقية.
- الأمراض المنتشرة (فيبروميالجيا/ألم عضلي/DOMS) تُخصم درجاتها عند وجود موقع/عضو محدد.

### 5) رعاية ذاتية متخصّصة لكل حالة/عضو/منطقة (لا تكرار)
- ترتيب: نصائح الحالة (أفضل 2) → نصائح العضو → نصائح المنطقة → نصيحة أمان → تعبئة بنصائح عامة متنوعة حسب درجة الفرز.
- إزالة التكرار + حد أقصى 6 نصائح.

### 6) فرز طبي أدق
- الزائدة والبنكرياس = «قريب» (soon)، المرارة = «روتيني».

## النتيجة بعد الإصلاح
- «شمال السرة» → أعضاء: الأمعاء، المبيض | أمراض: K58، K57، A09، N83.2 | رعاية ذاتية متنوعة ومرتبطة.
- «يمين السرة» → أعضاء: الزائدة، الأمعاء | المرض الأول: K35 التهاب الزائدة | الفرز: قريب.
- «تحت السرة» → أعضاء: المثانة، الرحم، الأمعاء.

## الملفات في هذا الأرشيف
| الملف | الحالة |
|---|---|
| `services/aiAssistant/engine.ts` | معدّل |
| `services/aiAssistant/lexicon.ts` | معدّل |
| `services/medical/diseaseLibrary.ts` | معدّل |
| `data/medical/organConditions.json` | جديد |
| `scripts/test-engine.mjs` | جديد (أداة اختبار) |

## طريقة الاختبار
```bash
cd bpm
node scripts/test-engine.mjs "حاسس بوجع في بطني على شمال السرة"
node scripts/test-engine.mjs "وجع في بطني على يمين السرة"
```
