# إصلاحات الجولة الثانية — BodyMap Pain

## المشكلتان اللتان أبلغت عنهما
1. ظهور نص إنجليزي خام `results.assessmentHint` في شاشة النتائج (مفتاح ترجمة ناقص).
2. تكرار نفس «الأسباب الشائعة» العامة (إجهاد/شد في الأنسجة الرخوة، وضعية، كدمة) في **272 من 317** عضلة — أي نفس الحل لأغلب المشاكل.

## الحلول المطبّقة

### 1) إصلاح مفتاح الترجمة الناقص
- أُضيف `results.assessmentHint` إلى `locales/ar.json` و`en.json` و`fr.json`، فلم يعد يظهر النص الخام.

### 2) أسباب وتوصيات وتحذيرات مخصّصة لكل منطقة (بدل التكرار)
- كانت **5 قوائم فقط** لأسباب مكرّرة على 317 عضلة. الآن أصبحت **23 قائمة** — واحدة لكل مجموعة عضلية، بمحتوى طبي دقيق.
- أُضيفت **توصيات مخصّصة** لكل مجموعة (مثال: أسفل الظهر ← تنبيه لفقدان التحكم بالبول/البراز؛ الصدر ← تنبيه لألم القلب؛ السمانة ← تنبيه للجلطة).
- رُفعت **العلامات التحذيرية** من 45 إلى **69** عضلة (أُضيفت لمناطق البطن، أسفل الظهر، الصدر، السمانة، الرأس).

### أمثلة على الفرق
| المنطقة | قبل (مكرّر) | بعد (دقيق) |
|---|---|---|
| البطن | إجهاد/شد في الأنسجة الرخوة | شد من تمارين البطن · فتق سُري/إربي · غازات أو التهاب معدة · سعال شديد |
| الصدر | نفس الأسطر | شد صدري · التهاب غضروف الضلع · ارتجاع مريئي · ألم قلبي محتمل |
| أسفل الظهر | نفس الأسطر | شد قطني · انزلاق غضروفي · رفع ثقل خاطئ · مشكلة كلى |
| الركبة | نفس الأسطر | التهاب وتر · إصابة غضروف/رباط · التهاب مفصل/خشونة · ثني طويل |

## الملفات المعدّلة/المضافة في هذا الأرشيف
| الملف | الحالة | الوصف |
|---|---|---|
| `locales/ar.json` | معدّل | إضافة assessmentHint |
| `locales/en.json` | معدّل | إضافة assessmentHint |
| `locales/fr.json` | معدّل | إضافة assessmentHint |
| `data/anatomyPainMap.json` | معدّل | أسباب/توصيات/تحذيرات مخصّصة لكل منطقة |
| `scripts/patch-anatomy-causes.mjs` | جديد | سكربت توليد الأسباب المخصّصة |
| `services/aiAssistant/engine.ts` | معدّل (جولة 1) | إصلاح تشخيص الأعضاء والتكرار |
| `services/aiAssistant/lexicon.ts` | معدّل (جولة 1) | مواقع البطن + الأعراض |
| `services/medical/diseaseLibrary.ts` | معدّل (جولة 1) | دمج الأمراض العضوية |
| `data/medical/organConditions.json` | جديد (جولة 1) | 15 حالة عضوية |
| `scripts/test-engine.mjs` | جديد (جولة 1) | أداة اختبار المحرّك |

## التحقق
- `npx tsc --noEmit` نظيف.
- `npm run build:web` نجح.
- عدد قوائم الأسباب المتميّزة: 5 ← 23.
