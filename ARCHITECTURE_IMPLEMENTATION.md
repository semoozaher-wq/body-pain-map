# دليل المعمارية المنفّذة (Architecture Implementation)

هذا الملف يوضّح كيف طُبّقت طبقات المعمارية المقترحة داخل هذا المستودع (Expo / React Native + TypeScript)، مع الحفاظ على المبدأ الأساسي: **كل النواتج احتمالات إرشادية وليست تشخيصًا طبيًا**.

## الطبقات المنفّذة

| الطبقة | الملف | الوصف |
| --- | --- | --- |
| خط الدفاع الأول — فحص جودة الصورة | `services/clinicalAnalysis.js` → `assessImageQuality` | تباين لابلاسيان للكشف عن الضبابية + فحص الأبعاد + فحص الإضاءة (مظلمة/مفرطة السطوع)، وإرجاع رسالة عربية تطلب إعادة التصوير. |
| خط الدفاع الأول — الأسئلة الديناميكية | `services/clinicalAnalysis.js` → `buildFollowUpQuestions` | أسئلة المدة والحرارة والشدة دائمًا، مع أسئلة إضافية مشروطة بالسياق (جلد / مفصل / حلق). |
| خط الدفاع الثاني — النموذج اللغوي/الرؤية الطبي | `services/visionAnalysis.ts` | جسر اختياري إلى نقطة نهاية متعدّدة الوسائط. معطّل افتراضيًا، والمفتاح يُقرأ من متغيّرات البيئة فقط. |
| خط الدفاع الثالث — الفرز والربط | `services/clinicalAnalysis.js` → `runRuleBasedTriage` و `mapSpecialty` و `mergeWithModelPossibilities` | الفرز القائم على القواعد يُنفّذ **قبل** أي إخراج للنموذج ويرفع درجة الخطورة عند علامات الإنذار، ثم يوجّه للتخصص المناسب. |
| الواجهة التجريبية | `components/ClinicalAnalysisPanel.tsx` | تنفّذ الثلاث مراحل كاملة (فحص جودة ← أسئلة ← فرز) في المتصفح. |
| إخلاء المسؤولية الثابت | `components/PersistentDisclaimer.tsx` | شريط إخلاء مسؤولية يظهر في كل الشاشات. |

## الخصوصية والامتثال

- لا تُرفع الصور تلقائيًا؛ الفحص يتم محليًا على الجهاز/المتصفح.
- لا مفاتيح مكتوبة داخل الشيفرة. المفتاح يُقرأ من: `EXPO_PUBLIC_MEDICAL_VISION_ENDPOINT` و `EXPO_PUBLIC_MEDICAL_VISION_KEY`.
- لا تُخزَّن صور دون تشفير: هذه النسخة لا تحفظ أي صور إطلاقًا؛ السجل المحلي يخزّن نصًا فقط عبر `AsyncStorage`.
- الإخلاء الطبي مذكور في `TERMS.md` و`PRIVACY.md` وشاشات التطبيق.

## المتغيّرات البيئية

```bash
# اختياري: تفعيل التحليل الآلي للصور. لا تضع أي مفتاح داخل الشيفرة أو في المستودع.
EXPO_PUBLIC_MEDICAL_VISION_ENDPOINT=
EXPO_PUBLIC_MEDICAL_VISION_KEY=
```

## التشغيل والتحقق

```bash
npm ci
npm run typecheck
npm test                      # يشمل اختبارات الفرز وفحص جودة الصورة الجديدة
npm run build:web
npm run test:e2e
```

## تحذير

المحتوى تعليمي وصفي فقط. الفرز القائم على القواعد لا يشخّص أي حالة، وقد يتشابه أكثر من حالة في نفس الأعراض. غياب التنبيه لا يعني أن الحالة آمنة، ويلزم مراجعة طبيب مؤهل قبل أي إطلاق طبي.
