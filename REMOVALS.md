# ملفات يجب حذفها من المستودع

هذه الملفات كانت نسخًا مكررة أو ملفًا فارغًا، وتم حذفها من نسخة الإصلاح:

- `constants/Button.tsx` — النسخة الصحيحة موجودة في `components/Button.tsx`.
- `constants/Card.tsx` — النسخة الصحيحة موجودة في `components/Card.tsx`.
- `constants/Accordion.tsx` — النسخة الصحيحة موجودة في `components/Accordion.tsx`.
- `constants/Header.tsx` — النسخة الصحيحة موجودة في `components/Header.tsx`.
- `useTheme.ts` — ملف فارغ؛ الملف الصحيح هو `hooks/useTheme.tsx`.

احذف هذه المسارات من GitHub إذا كنت ترفع الملفات يدويًا. لا تحذف ملفات `components/` أو `hooks/useTheme.tsx`.
